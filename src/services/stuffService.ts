const stuffEndPoint = "users/";

const stuffService = {
  fetchAll: async () => {},

  create: async (payload: {
    _id: string;
    email: string;
    password: string;
  }) => {},
};

export default stuffService;
